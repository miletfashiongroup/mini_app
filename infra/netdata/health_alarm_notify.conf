# Netdata notifications (Telegram) -- placeholders only.
SEND_TELEGRAM="YES"
TELEGRAM_BOT_TOKEN="${NETDATA_TELEGRAM_BOT_TOKEN}"
TELEGRAM_CHAT_ID="${NETDATA_TELEGRAM_CHAT_ID}"
DEFAULT_RECIPIENT_TELEGRAM="${NETDATA_TELEGRAM_CHAT_ID}"

# Avoid noisy defaults; tune thresholds in health.d/ if needed.
SEND_SLACK="NO"
SEND_EMAIL="NO"
